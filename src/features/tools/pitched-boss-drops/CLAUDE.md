# Pitched Boss Drops

Drops are events (`id`, `characterId`, `characterName`, `itemId`, `channel`, `date`, `timestamp`) — not per-character toggles.

`characterID` isn't a reliable unique key — treat `characterName` as canonical identity for display/filtering.
