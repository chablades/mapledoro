/** Shown once under the boss grid rather than as a `title` on every Cleared
 *  chip: a native tooltip is hover-only and unreachable on touch. */
export const CLEARED_HINT =
  "Mark a boss as cleared if you have already killed it since the last reset.";
